# 종가예측게임 — 앱스토어/플레이스토어 출시 가이드

## 0. 먼저 확인할 것 — 스토어 정책 리스크 (가장 중요)

출시 전에 반드시 짚고 가야 할 부분이에요. 검색해서 최신 정책을 직접 확인했습니다.

**Google Play 정책**은 사용자가 실제 현금(또는 현금으로 산 인앱 상품)으로 내기를 걸어 실제 금전적 가치가 있는 상품을 얻는 것을 금지합니다. 다만 <cite index="13-1">위 도박 앱 자격요건을 충족하지 않는 앱이라도, 실제 현금으로 내기를 하는 게 아니라 아래 설명된 "게임화된 포인트 제도" 요건을 충족하는 프로그램은 예외로 허용됩니다.</cite>

지금 만든 구조(포인트는 실제 돈으로 구매 불가, 회원가입·출석·추천으로만 획득, 쿠폰도 현금이 아닌 상품권)는 이 예외 범주에 들어갈 가능성이 있어요. 하지만 "투자풀에 포인트를 걸고 배당을 나눠 갖는다"는 표현 자체가 베팅처럼 보일 수 있어서, **제출 전에 반드시**:
- Google Play 개발자 정책센터의 "게임화된 포인트 제도" 세부 요건 원문을 직접 정독하고 그 기준에 정확히 맞는지 확인
- Apple 심사 가이드라인 3.1.1(인앱 구매)과 도박/콘테스트 관련 조항도 함께 확인 (Apple은 "포인트를 실물 가치로 교환"하는 구조에 특히 엄격합니다)
- 가능하면 게임/사행성 규제에 밝은 국내 변호사 자문을 한 번은 받아보시길 권해요 (게임산업진흥법상 사행성 게임물 해당 여부)

**리스크를 낮추는 실질적인 방법**: 심사 제출용 앱 설명에서 "베팅", "배당" 같은 표현을 피하고 "예측 참여형 리워드 프로그램"처럼 서술하고, 쿠폰 금액 상한을 낮게 유지하고, 포인트의 현금 구매 경로를 절대 열지 않는 것이 안전합니다.

---

## 1. 앱스토어 등록 방법 — Capacitor로 감싸는 걸 추천해요

지금 있는 `user-app`은 이미 완성된 웹앱(HTML+JS, REST API 호출)입니다. 이걸 완전히 새로 앱으로 다시 만들 필요 없이, **Capacitor**(Ionic 팀이 만든 오픈소스)로 감싸면 하나의 코드베이스로 iOS/Android 둘 다 만들 수 있어요.

**왜 Capacitor를 추천하는가**
- 기존 `user-app` 코드를 거의 그대로 씁니다 (완전 재작성 불필요)
- **가장 큰 장점**: 네이티브 껍데기(Capacitor 셸) 안에서 여러분 서버에 있는 웹 콘텐츠를 그대로 불러오는 구조로 만들면, **UI/기능 업데이트는 서버에 새 코드를 배포하기만 하면 끝나고 스토어 재심사가 필요 없어요.** 네이티브 플러그인 추가나 앱 아이콘 변경처럼 "네이티브 셸 자체"를 바꿀 때만 다시 빌드해서 스토어에 제출하면 됩니다. (자세한 건 6번 항목에서)
- 푸시 알림, 생체인증, 카메라 같은 네이티브 기능이 필요해지면 플러그인으로 나중에 추가 가능

**대안**: React Native/Flutter(완전 네이티브 재작성, 성능/UX는 더 좋지만 지금 단계에서는 과함), PWA+TWA(Android만 가능, iOS는 제약이 많아 비추천)

**필요한 것**
- Apple Developer Program: **연 $99**, 앱 심사·배포에 필요. Xcode 빌드는 macOS가 필요한데, 로컬에 맥이 없으면 Codemagic 같은 클라우드 CI로 macOS 없이도 빌드 가능
- Google Play Console: **최초 1회 $25** (연 갱신 없음)
- 두 계정 모두 개설 후 실제 심사 승인까지 보통 1~3일 소요

---

## 2. 회원가입 인증 — 네이버 관련 옵션 정리

"네이버에서 인증"이 두 가지 다른 걸 의미할 수 있어서 구분해서 설명드릴게요.

| 구분 | 무엇인가 | 어뷰징 방지 효과 |
|---|---|---|
| **네이버 아이디로 로그인** (OAuth) | "네이버로 3초 만에 가입" 같은 소셜 로그인. 네이버 계정만 있으면 인증 완료 | ⚠️ 낮음 — 네이버 계정도 이메일처럼 여러 개 만들 수 있어서, 지난번에 뺀 구글 로그인과 동일한 약점을 그대로 가집니다 |
| **본인인증** (PASS·나이스평가정보·다날 등) | 통신 3사와 연동해서 실명+주민등록번호 기반으로 "이 사람이 실제로 존재하고 이 번호를 쓴다"를 증명 | ✅ 강함 — 지금 만든 휴대폰 OTP보다 한 단계 더 강력한 실명 인증. 통신사 가입자 기준이라 사실상 1인 1계정이 강제됨 |

**추천**: 지금 자체 구현한 휴대폰 OTP(SMS 인증번호)를 실 서비스 출시 시점에 **PASS 인증(통신 3사 통합인증) 또는 나이스평가정보 본인인증 API**로 업그레이드하는 걸 권해요. 코드 구조상 `src/verification/phone-auth.service.ts`의 검증 로직만 이 API 응답으로 교체하면 되고, 나머지(신규 계정 unique 제약, 추천인 보너스 연동 등)는 그대로 재사용됩니다.

**네이버 로그인은 "편의 기능"으로 추가하는 걸 추천**: 가입 자체의 신뢰 기준(어뷰징 방지)은 본인인증이 맡고, 네이버/카카오 로그인은 그 위에 "로그인을 편하게 해주는" 용도로만 쓰는 거예요. 즉 네이버 로그인으로 들어와도 쿠폰 교환·추천보너스는 여전히 본인인증 완료 후에만 풀리는, 지금 구글 관련 로직과 똑같은 게이팅 구조를 그대로 적용하면 됩니다.

---

## 3. 클라우드 & 문자/쿠폰 발송 서비스 추천

네이버 쪽으로 방향을 잡으신 김에, **네이버클라우드플랫폼(NCP)**으로 통일하면 로그인부터 인프라, 문자 발송까지 한 생태계 안에서 관리가 편해집니다.

**클라우드 호스팅**
| 서비스 | 장점 | 이럴 때 |
|---|---|---|
| **네이버클라우드플랫폼(NCP)** | 국내 리전, 원화 결제, SENS(문자)·Cloud DB·Object Storage 등 필요한 게 다 있음, 스타트업 크레딧 프로그램 있음 | 네이버 로그인/한국 사용자 위주라면 가장 자연스러운 선택 |
| **NHN Cloud(Toast)** | 국내 게임사들이 많이 씀, SMS/PG/푸시 번들 | 게임성 서비스에 특화된 인프라를 원할 때 |
| **AWS (서울 리전)** | 가장 성숙한 생태계, 압도적 자료/커뮤니티 | 나중에 해외 확장이나 대규모 트래픽을 염두에 둘 때 |

**문자(SMS/알림톡) 발송**
- **네이버클라우드 SENS**: NCP를 쓴다면 가장 자연스러운 선택. SMS + 카카오 알림톡을 하나의 API로 처리
- **알리고(Aligo)**: 국내 스타트업들이 많이 쓰는 가벼운 SMS API, 저렴하고 연동 쉬움
- **카카오 알림톡**: 건당 비용이 일반 SMS(20~40원)보다 훨씬 저렴(9~15원 수준)해서, 인증번호·쿠폰 발급 문자량이 많아지면 알림톡으로 전환하는 걸 추천 (발송대행사: 비즈엠, 인포뱅크, NHN Toast 등 통해 신청)
- 지금 코드의 `SmsProvider` 인터페이스(`src/verification/sms-provider.ts`)에 위 서비스들의 실제 API 호출 코드만 넣으면 바로 교체 가능하도록 미리 만들어뒀어요

**실제 쿠폰(상품권) 자동 발급**
- 지금은 관리자가 쿠폰번호를 손으로 입력하는 구조인데, 규모가 커지면 실제 상품권 발급 API와 연동하는 걸 추천해요
- **KT엠하우스(기프티쇼비즈)**, **다날 상품권 API**, **SK플래닛 기프티콘 비즈니스** — 이런 곳과 제휴하면 관리자가 쿠폰을 "등록"하는 게 아니라, 사용자가 교환하는 순간 API로 실제 상품권 코드를 실시간 발급받아 지급하는 구조로 바꿀 수 있어요

---

## 4. 한국 증시 기반 종목 구성 (반영 완료)

`prisma/seed.ts`의 종목 리스트를 코스피/코스닥 대표 종목 위주로 교체했어요: 삼성전자, SK하이닉스, NAVER, 카카오, LG에너지솔루션, 현대차, 기아, POSCO홀딩스, 삼성바이오로직스, 셀트리온, KB금융, 신한지주, 에코프로비엠, 알테오젠, 에코프로. 시가총액 상위 + 업종 대표주 위주로 골랐고, 관리자 대시보드에서 언제든 종목을 추가/변경할 수 있는 구조는 그대로예요.

---

## 5. 실시세 API 추천

`src/prices/price.service.ts`에 이미 교체 가능한 구조(`PriceProvider` 인터페이스)로 만들어뒀는데, 실 API로 채울 때 추천하는 순서예요.

1. **한국투자증권 KIS Developers (Open API)** — 1순위 추천. 무료, 공식, REST+웹소켓 실시간 시세 제공, 국내/해외 둘 다 지원, 개발 문서가 잘 되어 있어요. 코드 안에 이미 이 API 기준 예시 스켈레톤을 주석으로 넣어뒀습니다.
2. **키움증권 OpenAPI+** — 강력하지만 Windows 전용 COM 방식이라 리눅스 서버에서 돌리는 지금 구조와는 궁합이 안 좋아요 (별도 Windows 브릿지 서버가 필요)
3. **한국거래소(KRX) 정보데이터시스템 / 금융위원회 공공데이터포털** — 정산 시점의 "공식 종가"를 한 번 더 교차검증하는 용도로 함께 쓰는 걸 추천 (실시간용은 아니고, 확정 종가 검증용)
4. 네이버/다음 금융 페이지를 크롤링하는 방식은 **추천하지 않아요** — 이용약관 위반 소지가 있고 언제든 구조가 바뀌어 깨질 수 있습니다

---

## 6. 나중에 업그레이드하는 방법

**핵심 구분: 백엔드/웹콘텐츠 업데이트 vs 네이티브 셸 업데이트**

| 바꾸는 것 | 필요한 절차 | 스토어 재심사 |
|---|---|---|
| 게임 로직, UI 문구, 새 화면, 버그 수정, DB 스키마 변경 | 서버에 새 백엔드 배포 + (Capacitor가 원격 URL을 로드하는 구조라면) 프런트 코드도 서버에 배포 | ❌ 불필요 |
| 앱 아이콘, 권한, 네이티브 플러그인 추가(푸시 알림 등), 스토어에 노출되는 앱 이름/설명 | 앱 버전을 올려서 다시 빌드 → 스토어에 재제출 | ✅ 필요 (Apple 보통 1~2일, Google은 대체로 더 빠름) |

**배포 파이프라인 추천**
- **백엔드**: GitHub Actions로 `main` 브랜치에 푸시되면 자동으로 테스트 → Docker 이미지 빌드 → 서버 배포 → `npx prisma migrate deploy` 실행. 이미 만들어둔 `Dockerfile`/`docker-compose.yml`이 이 파이프라인의 기반이 됩니다.
- **앱(Capacitor)**: Codemagic이나 GitHub Actions + Fastlane 조합으로 iOS/Android 빌드를 자동화하고 스토어 업로드까지 자동화하는 걸 추천해요. 사람이 손으로 Xcode/Android Studio를 매번 여는 것보다 훨씬 안정적입니다.

**DB 마이그레이션은 항상 "추가 먼저, 삭제는 나중에"**
- 예: 컬럼을 지워야 한다면 (1) 먼저 새 배포에서 그 컬럼을 안 쓰게 코드만 바꾸고 배포 → (2) 문제없이 돌아가는 거 확인 후 다음 배포에서 실제로 컬럼 삭제하는 마이그레이션 적용. 한 번에 코드 변경 + 컬럼 삭제를 같이 하면, 배포 중간에 잠깐이라도 예전 코드가 살아있는 서버 인스턴스가 있으면 그 서버가 에러를 냅니다.

**버전 관리**
- 백엔드 API: 지금처럼 `/predictions`, `/coupons` 같은 경로를 쓰다가 나중에 호환 안 되는 큰 변경이 필요하면 `/v2/predictions`처럼 버전을 분리하는 걸 추천 (기존 앱 사용자가 업데이트 안 해도 당분간 계속 동작하게)
- 앱: iOS `CFBundleVersion`/Android `versionCode`는 배포할 때마다 반드시 올리고, 사용자에게 보이는 마케팅 버전(1.2.3 같은 semantic versioning)도 같이 관리

**단계적 배포로 위험 줄이기**
- Google Play는 "단계적 출시"(예: 처음엔 사용자 10%에게만 새 버전 노출 → 문제 없으면 확대) 기능이 있고, Apple도 "단계적 릴리즈"가 있어요. 큰 업데이트일수록 이 기능을 쓰는 걸 권해요 — 문제가 생겨도 전체 사용자가 아니라 일부만 영향을 받습니다.

---

## 다음으로 진행하면 좋은 것
1. Capacitor로 `user-app`을 감싸는 프로젝트 스캐폴딩 (원하시면 바로 만들어드릴게요)
2. PASS/나이스평가정보 본인인증 API 연동 (지금 SMS OTP 구조를 그대로 확장)
3. 네이버 로그인(OAuth) 연동 — 전에 만든 구글 로그인 게이팅 구조를 그대로 재사용
4. NCP SENS 또는 알리고로 `SmsProvider` 실제 구현체 교체

---

# 12단계: 사업자등록 관련 확인 + 토스증권 API 실연동

## 사업자등록 — 자택 주소로 가능한가

**결론: 가능합니다.** 부가가치세법상 사업장은 원칙적으로 별도 공간이 필요하지만, **온라인 통신망을 이용한 사업(전자상거래업·통신판매업)은 예외적으로 거주지 주소를 사업장으로 등록할 수 있습니다.** 식당·제조업처럼 물리적 공간이 필요한 업종만 아니면 되고, 지금 만드는 서비스는 정확히 이 예외에 해당해요.

**업태/종목 추천**: 지금 하시려는 사업(포인트를 쌓아 모바일 상품권으로 교환)은 실제로 **도소매업(전자상거래업)** 이 정확한 분류예요. 홈택스에서 사업자등록 신청 시:
- 업태: **도소매업** (또는 정보통신업도 함께 등록 가능 — 앱/소프트웨어 운영이므로)
- 종목: **전자상거래업** (통신판매업)

**절차 순서**
1. **사업자등록**: 홈택스(온라인) 또는 관할 세무서 방문 — 사업장 주소만 있으면 바로 가능, 별도 서류 거의 없음
2. **통신판매업신고**: 사업자등록 이후 진행. 사업장 관할 시/군/구청 또는 정부24에서 온라인 신청 가능. 이때 **"구매안전서비스(에스크로) 이용확인증"** 이 필요한데, PG사(결제대행사) 가입 시 대부분 자동으로 발급해줍니다 (카페24페이먼츠, 토스페이먼츠 등)

**한 가지 주의할 점**: 지금 살고 계신 집이 **전세/월세**라면, 이론적으로는 임대인의 동의(또는 최소한 문제 삼지 않을 것)가 필요할 수 있어요. 실무적으로는 온라인 사업(방문객 없음, 간판 없음)은 문제되는 경우가 드물지만, 임대차계약서에 "주거 목적 외 사용 금지" 조항이 있다면 분쟁 소지가 될 수 있으니 계약서를 한 번 확인해보시는 걸 권해요. 자가라면 이 문제는 없습니다.

## PASS/나이스 본인인증·기프티쇼비즈가 자택 개인사업자와 계약해주는지

이건 제가 확답드리기 어려운 부분이에요 — **각 회사의 자체 심사 기준**이라 공개된 "최소 요건" 같은 게 없거든요. 다만 일반적인 경향은 이래요:

- **본인인증 API(나이스평가정보, 다날 등)**: 개인사업자·소규모 스타트업도 계약 자체는 가능한 경우가 많아요. 다만 지금 서비스가 예측·베팅 성격을 띠고 있어서, 계약 심사 과정에서 **서비스 모델 자체에 대한 검토**(자금세탁방지/KYC 관점)가 있을 수 있어요. 이 부분은 사업자 형태보다 서비스 성격이 더 중요한 심사 포인트가 될 거예요.
- **기프티쇼비즈**: 홈페이지(biz.giftishow.com)에 문의 폼이 있으니, 정식 계약 전에 먼저 "개인사업자도 API 연동이 가능한지" 직접 문의해보시는 걸 추천해요. 최소 발송량 조건이 있을 수도 있어요.

**실전 팁**: 사업자등록증을 먼저 발급받고, 두 회사 영업팀에 각각 문의 메일/전화로 "개인 도소매업 사업자인데 계약 가능한지, 최소 조건이 있는지"를 먼저 확인하시는 게 시간 낭비를 줄이는 방법이에요. 원하시면 문의 메일 초안을 같이 작성해드릴 수 있어요.

## 토스증권 API — 실제 연동 완료

토스증권 공식 API 문서(developers.tossinvest.com)를 직접 확인해서 정확한 스펙으로 구현했어요.

**만든 것**: `src/prices/toss-price.provider.ts`
- OAuth 2.0 Client Credentials로 토큰 발급 (만료 전 자동 캐시/갱신)
- 일봉 캔들 API(`GET /api/v1/candles`)로 당일/전일 종가를 가져와 등락률을 계산 (토스 API가 등락률을 직접 주지 않아서, 종가 두 개를 비교하는 방식으로 계산했어요)
- **`.env`에 키만 채우면 자동으로 활성화**됩니다 — `price.module.ts`가 `TOSS_CLIENT_ID`/`TOSS_CLIENT_SECRET` 존재 여부를 보고 Mock과 실제 API 중 알아서 골라 씁니다. 코드를 더 안 건드려도 돼요.

**발급 방법**
1. 토스증권 앱에서 계좌가 없다면 먼저 비대면 계좌 개설 (1분 정도 걸려요)
2. PC 브라우저로 [토스증권 웹(WTS)](https://tossinvest.com) 로그인
3. 설정 → **Open API** 메뉴 진입
4. `client_id`, `client_secret` 발급
5. 같은 화면 하단 **"허용 IP 관리"**에서, 서버를 배포할 IP를 등록해야 해요 (등록 안 하면 403 에러) — 로컬 개발 중이면 지금 컴퓨터의 공인 IP를 등록하시면 됩니다 (검색창에 "내 IP 확인" 치면 바로 나와요)
6. 발급받은 두 값을 `.env`에 넣기:
   ```
   TOSS_CLIENT_ID=발급받은값
   TOSS_CLIENT_SECRET=발급받은값
   ```
7. 서버 재시작 → 스케줄러가 게임 정산 시 자동으로 실제 시세를 사용

**테스트 방법**: 관리자 대시보드에서 게임을 만든 뒤, `POST /admin/games/:id/settle`을 수동으로 호출하는 대신, 마감시각을 가깝게 잡아서 스케줄러(5분 주기)가 자동으로 락+정산하는 걸 기다려보거나, 서버 로그에서 `토스증권 토큰 발급 실패` 같은 에러가 없는지 먼저 확인해보세요.

## 실행 방법 (1~11단계에 이어서)
```bash
npm install
npx prisma generate
npm run start:dev
```
스키마 변경은 없어서 마이그레이션은 필요 없어요 — `.env`에 토스증권 키만 넣고 서버 재시작하면 됩니다.

---

# 13단계: 실시간 시세 화면 표시

## 이전에 없던 것 — 왜 시세가 안 보였는지
`PriceService`는 지금까지 **게임 정산 시점에 한 번, 서버 내부에서만** 쓰였어요 (스케줄러가 마감된 게임의 등락률을 계산할 때). 화면에 실시간으로 시세를 보여주는 API는 따로 없었습니다.

## 추가한 것
- `PriceProvider` 인터페이스에 `getQuote()` 추가 — 현재가 + 등락률을 함께 반환
- `TossPriceProvider.getQuote()`: 토스증권 `/api/v1/prices`(현재가) + `/api/v1/candles`(전일 종가)를 조합해서 실시간 등락률 계산
- `MockPriceProvider.getQuote()`: 키가 없을 때는 랜덤 시세로 화면 개발 가능
- **`GET /stocks/:id/quote`** — 공개 API (인증 불필요), 관리자 대시보드와 `user-app` 둘 다 이걸 호출
- **관리자 대시보드**: 게임 관리 › 등록된 게임 목록의 각 게임 카드에 현재가+등락률이 표시됩니다
- **`user-app`**: 홈 화면의 게임 카드, 예측 화면의 종목 상세 둘 다에 현재가+등락률이 표시됩니다

## 실행 방법
```bash
npm install
npx prisma generate
npm run start:dev
```
스키마 변경 없음 — 서버만 재시작하면 바로 시세가 보입니다. `TOSS_CLIENT_ID`/`SECRET`이 없으면 자동으로 Mock 랜덤 시세가 뜨니, 아직 토스 키 발급 전이어도 화면 확인은 바로 가능해요.

---

# 15단계: 적중구간 재정의 / 0% 보합 특수처리 / 개인정보 동의 / 자동로그인 / 관리자 포인트 회수

## 적중 구간 재정의 (구간 방식)
기존 "가장 가까운 버킷으로 반올림" 방식에서, 요청하신 대로 **"선택한 값이 구간의 상한"** 방식으로 바꿨어요.

- 하단계에서 **+1%**를 선택 → 적중 구간은 **0% 초과 ~ +1% 이하**
- **+2%**를 선택 → **+1% 초과 ~ +2% 이하**
- 최상단 버킷(예: +6%)은 상한 없이 **+5% 초과 전부** 적중
- 음수도 동일하게 대칭 적용
- **0%는 선택지에서 완전히 제거**했습니다 (`user-app`의 칩 목록에서도 삭제)
- 중급(0.5% 단위)·상급(0.2% 단위)도 같은 공식을 그대로 재사용합니다 (`src/common/tier-rules.ts`의 `isWin()` 하나로 세 난이도 모두 처리)

**사용자에게 구간 표시**: 난이도·방향을 선택하면 화면에 바로 "적중 구간: 0% 초과 ~ +1% 이하" 같은 문구가 떠서, 무엇을 맞혀야 적중인지 명확히 보여줘요.

## 0%(보합) 특수 처리
실제 등락률이 **정확히 0.00%**로 마감되면, 어떤 예측도 적중할 수 없도록 판정 로직 자체에서 막았어요 (`if (actual === 0) return false`). 그 결과 별도 분기 없이도 기존 정산 로직이 "적중자 없음 → 전액 다음 회차로 이월" 처리를 자동으로 수행합니다. `user-app` 예측 화면에도 이 규칙을 안내하는 문구를 추가했어요.

## 회원가입 개인정보 동의
`user-app` 회원가입 화면에 **"이용약관 및 개인정보 수집·이용 동의(필수)"** 체크박스를 추가했어요. "전문 보기"를 누르면 수집 항목·목적·보유기간이 펼쳐지고, **왜 이 정보가 필요한지**(쿠폰 발송, 1인 다중계정 방지)를 명시했습니다. 체크하지 않으면 가입이 안 되고, 서버(`SignupDto.privacyConsent`)에서도 `true`가 아니면 거부하도록 이중으로 막아뒀어요. 동의 시각은 `User.privacyConsentedAt`에 기록됩니다.

⚠️ 이 문구는 제가 초안으로 작성한 것이라, 실제 서비스 출시 전에는 개인정보보호법 전문가(변호사·개인정보보호 담당자) 검토를 받으시길 권해요.

## 자동 로그인
로그인 화면에 **"자동 로그인"** 체크박스를 추가했어요. 체크하고 로그인하면 리프레시 토큰을 브라우저에 저장해뒀다가, 다음에 앱을 다시 열었을 때 자동으로 로그인 상태를 복원합니다. 체크 안 하면 저장 안 하고, 로그아웃하면 저장된 것도 지워져요.

⚠️ 이 기능은 `localStorage`를 사용해서, **Claude.ai 미리보기 화면 안에서는 동작하지 않아요.** 파일을 다운로드해서 실제 브라우저나 서버로 열었을 때만 정상 작동합니다 (지금 배포하시는 방식이라면 문제없어요).

## 관리자 포인트 회수 기능
관리자 대시보드 › 회원 관리 화면에서, 각 회원 카드에 포인트 입력란과 사유 입력란이 생겼어요. 어뷰징이 의심되면 포인트를 입력하고 사유를 적은 뒤 **"회수"**를 누르면 그만큼 차감됩니다 (보유 포인트보다 많이 회수하려 하면 자동으로 보유량만큼만 회수돼요). **"지급"**으로 보정 지급도 가능해요. 모든 조정은 `points_ledger`에 `admin_adjustment` 사유와 함께 관리자가 적은 메모까지 남아서, 나중에 왜 조정했는지 추적할 수 있습니다.

## 실행 방법 (1~14단계에 이어서)
```bash
npm install
npx prisma generate
npx prisma migrate dev --name win_ranges_privacy_clawback
npm run start:dev
```

⚠️ `privacyConsentedAt`이 새로 필수 개념으로 들어가긴 했지만 nullable이라 기존 계정엔 영향 없어요. 다만 혹시 마이그레이션 중 문제가 생기면 이번에도 `npx prisma migrate reset` 후 `npx ts-node prisma/seed.ts`가 가장 확실한 해결책이에요.

---

# 16단계: 무료 호스팅으로 배포해서 모바일에서 확인하기

지금까지는 전부 본인 컴퓨터(localhost)에서만 열어볼 수 있었는데, 이번엔 **진짜 인터넷 주소**로 배포해서 휴대폰 브라우저에서 바로 열어볼 수 있게 만들어드릴게요. 전부 무료 등급으로 가능합니다.

## 구성
| 역할 | 서비스 | 왜 |
|---|---|---|
| 백엔드(NestJS API) | **Render.com** (Web Service, Docker) | Dockerfile을 그대로 감지해서 배포, 무료 등급 제공 |
| 데이터베이스(PostgreSQL) | **Supabase** 또는 **Neon** | 무료 등급이 영구적(90일 제한 없음), 접속 정보(연결 문자열)를 바로 줌 |
| 화면(admin-dashboard, user-app) | **Cloudflare Pages** 또는 **Netlify** | 정적 HTML 파일을 그대로 올리면 HTTPS 주소가 바로 생김 |

**무료 등급의 한계**: Render 무료 웹서비스는 15분간 요청이 없으면 잠들고, 다음 요청 때 30초~1분 정도 깨어나는 데 시간이 걸려요(콜드 스타트). 테스트/데모 단계에서는 문제없지만, 실사용자가 많아지면 유료로 전환해야 해요 (아래 참고).

## 순서

**1) 데이터베이스 먼저 (Supabase)**
1. [supabase.com](https://supabase.com) 가입 → New Project 생성 (리전은 Northeast Asia/Seoul이 있으면 선택)
2. 프로젝트 생성 후 **Settings → Database → Connection string** 에서 `URI` 형식 연결 문자열을 복사 (비밀번호를 프로젝트 생성 시 입력한 값으로 채워야 함)
3. 이 값을 나중에 Render의 `DATABASE_URL` 환경변수에 넣을 거예요

**2) 백엔드 배포 (Render)**
1. 먼저 지금까지 받은 `backend` 폴더를 GitHub에 새 저장소로 올려주세요 (GitHub Desktop이나 `git init` → `git add .` → `git commit` → `git push` — `.gitignore`가 이미 있어서 `.env`는 자동으로 제외됩니다)
2. [render.com](https://render.com) 가입 → **New → Web Service** → 방금 올린 GitHub 저장소 선택
3. Render가 `Dockerfile`을 자동으로 감지합니다. Region은 Singapore(가장 가까운 무료 리전)로 선택
4. **Environment** 탭에서 `.env.example`에 있는 값들을 하나씩 등록 (`DATABASE_URL`은 1번에서 복사한 Supabase 값, `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET`은 무작위 문자열, `TOSS_CLIENT_ID`/`SECRET`도 있으면 등록)
5. Instance Type은 **Free** 선택 → Deploy
6. 배포가 끝나면 `https://내서비스이름.onrender.com` 같은 주소가 생깁니다 — 이게 API 주소예요
7. 배포 로그에서 `npx prisma migrate deploy`가 성공했는지, 그다음 `🚀 API 서버 실행 중`이 찍히는지 확인하세요
8. 최초 1회, Render Shell 탭(또는 로컬에서 `DATABASE_URL`을 Supabase 걸로 바꿔서)으로 `npx ts-node prisma/seed.ts`를 실행해 관리자 계정과 종목을 만들어주세요

**3) 화면 배포 (Cloudflare Pages)**
1. [pages.cloudflare.com](https://pages.cloudflare.com) 가입 → **Create a project → Upload assets** (Git 연동도 가능하지만 지금은 파일 업로드가 더 빨라요)
2. `user-app` 폴더를 통째로 업로드 → 배포되면 `https://xxxx.pages.dev` 주소가 생겨요
3. 같은 방식으로 `admin-dashboard` 폴더도 별도 프로젝트로 하나 더 만들어서 업로드
4. 각 사이트에 접속해서 **API 주소 입력란에 2번에서 받은 Render 주소**를 넣어주면 연결 완료

**4) 휴대폰에서 확인**
- `user-app`의 Cloudflare Pages 주소를 휴대폰 브라우저(사파리/크롬)로 열면 끝이에요
- 아이폰 사파리 기준: 공유 버튼 → "홈 화면에 추가"를 누르면 아이콘이 생겨서 앱처럼 실행돼요 (완전한 네이티브 앱은 아니지만, 심사 전에 실제 앱처럼 테스트하기엔 충분해요)

## 나중에 사용자가 많아지면 — 유료 전환 추천
- **네이버클라우드플랫폼(NCP)**: 이미 앞서 추천드린 것처럼, 네이버 로그인·SENS(문자)와 한 생태계로 관리하기 편해서 국내 서비스에 가장 자연스러운 선택이에요
- **AWS (서울 리전)**: 트래픽이 커지고 오토스케일링·글로벌 확장까지 고려한다면 가장 성숙한 선택지예요
- **Render 유료 플랜**: 지금 구성을 그대로 유지하면서 콜드 스타트 없이 상시 가동만 원한다면, 코드 변경 없이 Render 안에서 유료 플랜으로 버튼 하나로 전환할 수 있어요 (가장 손이 안 가는 방법)

작은 규모(수백~수천 명)까지는 Render 유료 플랜만으로도 충분히 버틸 수 있고, 그 이상으로 커지면 NCP/AWS로 옮기는 걸 권해요.

---

# 17단계: 난이도별 고정 투자금 / 베팅 횟수·쿨다운 제한 / 정산 날짜 지정 방식

## 변경 사항

**난이도별 고정 투자 포인트**
- 더 이상 사용자가 투자 포인트를 직접 고르지 않습니다. `TierConfig.fixedStake`(하 150P / 중 100P / 상 50P — `prisma/seed.ts`에서 설정)가 그대로 적용됩니다.
- 서버가 항상 이 값을 사용해서 차감하므로, 클라이언트가 다른 값을 보내려 해도 무시됩니다.
- 여러 적중자의 배당 비율 계산 로직은 그대로 재사용됩니다 — 스테이크가 전부 동일해지면서 자연스럽게 "적중자끼리 균등 분배"가 됩니다.

**난이도별 베팅 횟수·쿨다운 제한**
- 난이도(하/중/상)마다 **하루 최대 3회, 마지막 베팅 후 8시간이 지나야 재베팅 가능**하도록 제한했습니다 (`.env`의 `BET_DAILY_LIMIT`, `BET_COOLDOWN_HOURS`로 조정 가능).
- `GET /predictions/limits` — 난이도별 오늘 사용 횟수, 다음 베팅 가능 시각을 반환합니다. `user-app` 예측 화면에 이 정보가 실시간으로 표시되고, 조건이 안 되면 그 난이도는 아예 선택할 수 없게 잠깁니다.

**정산 날짜 지정 방식**
- 관리자가 게임을 만들 때 더 이상 정확한 시각을 입력하지 않고 **날짜(YYYY-MM-DD)만** 입력합니다.
- 서버가 그 날짜의 한국 증시 마감 시각(15:30 KST) + 5분 버퍼로 자동 계산해서 마감시각을 정합니다.
- 이후 흐름은 기존과 동일합니다: 스케줄러가 마감시각이 지나면 게임을 잠그고, 토스증권 API로 그 날짜의 확정 종가를 조회해서 자동 정산합니다.

## API 주소 변경
`admin-dashboard`, `user-app` 두 화면의 기본 API 주소를 `http://localhost:4000`에서 배포된 주소(`https://stock-game-ccga.onrender.com`)로 바꿨습니다.

## 실행 방법 (1~16단계에 이어서)
```bash
npm install
npx prisma generate
npx prisma migrate dev --name fixed_stake_bet_limits
npm run start:dev
```
로컬 테스트라면, 이번에도 `npx prisma migrate reset` 후 `npx ts-node prisma/seed.ts`가 가장 확실해요 (TierConfig에 새 컬럼 `fixedStake`가 추가됐습니다).

배포본(Render)에도 같은 방식으로 코드를 반영하고 GitHub에 push하면 자동 재배포되며, 이번엔 마이그레이션 파일이 이미 있으니 `prisma migrate deploy`가 자동으로 새 컬럼을 추가합니다. `TierConfig`는 seed 스크립트로만 값이 들어가니, 배포 후 Render Shell에서 `npx ts-node prisma/seed.ts`를 한 번 더 실행해서 `fixedStake` 값(150/100/50)을 반영해주세요.
